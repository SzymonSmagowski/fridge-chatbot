node_modules
.next
out
build
coverage
test-results
playwright-report

.env
.env.*
!.env.example

.git
.gitignore
.dockerignore
README.md
CLAUDE.md
AGENTS.md

# Wake-word ONNX files are downloaded fresh inside the builder stage,
# so don't ship the developer's cached copy through the build context.
public/wake-words/*.onnx
